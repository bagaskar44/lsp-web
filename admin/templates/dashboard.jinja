<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Dashboard Admin - Arcadia</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-danger mb-4">
        <div class="container">
            <a class="navbar-brand" href="#">Arcadia ADMIN</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link active" href="/dashboard">Pesanan Masuk</a>
                <a class="nav-link" href="/pengembalian">Pengembalian</a>
                <a class="nav-link" href="/buku">Kelola Buku</a>
                <a class="nav-link btn btn-light btn-sm text-dark ms-2" href="/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <h3>Daftar Pesana Masuk</h3>
        <p>Silakan periksa detail pesanan sebelum menyetujui.</p>

        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }}">{{ message }}</div>
                {% endfor %}
            {% endif %}
        {% endwith %}

        <table class="table table-hover mt-3">
            <thead class="table-light">
                <tr>
                    <th>Kode</th>
                    <th>Tanggal Pesan</th>
                    <th>Nama Peminjam</th>
                    <th>Jumlah Buku</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                {% for p in pesanan %}
                <tr>
                    <td>#{{ p['kode_pinjam'] }}</td>
                    <td>{{ p['tgl_pesan'] }}</td>
                    <td>{{ p['nama_peminjam'] }}</td>
                    <td>{{ p['jumlah_buku'] }} Buku</td>
                    <td>
                        <a href="/detail_pesanan/{{ p['kode_pinjam'] }}" class="btn btn-info btn-sm text-white">Periksa Detail</a>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="5" class="text-center">Tidak ada pesanan baru.</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>