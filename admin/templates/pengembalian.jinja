<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Pengembalian - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-danger mb-4">
        <div class="container">
            <a class="navbar-brand" href="#">Arcadia ADMIN</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/dashboard">Pesanan Masuk</a>
                <a class="nav-link active" href="/pengembalian">Pengembalian</a>
                <a class="nav-link" href="/buku">Kelola Buku</a>
                <a class="nav-link btn btn-light btn-sm text-dark ms-2" href="/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <h3>Daftar Peminjaman Aktif</h3>
        <p>Konfirmasi di sini jika peminjam mengembalikan buku melalui Drive-Thru.</p>

        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }}">{{ message }}</div>
                {% endfor %}
            {% endif %}
        {% endwith %}

        <table class="table table-bordered mt-3">
            <thead class="table-light">
                <tr>
                    <th>Kode</th>
                    <th>Peminjam</th>
                    <th>Tgl Ambil</th>
                    <th>Wajib Kembali</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                {% for k in list_kembali %}
                <tr>
                    <td>#{{ k['kode_pinjam'] }}</td>
                    <td>{{ k['nama_peminjam'] }}</td>
                    <td>{{ k['tgl_ambil'] }}</td>
                    <td>{{ k['tgl_wajibkembali'] }}</td>
                    <td>
                        <a href="/konfirmasi_kembali/{{ k['kode_pinjam'] }}" class="btn btn-success btn-sm" onclick="return confirm('Konfirmasi buku sudah dikembalikan?');">
                            Terima Pengembalian (Selesai)
                        </a>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="5" class="text-center">Tidak ada buku yang sedang dipinjam.</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>